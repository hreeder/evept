stages:
  - backend-characterUpdater-build
  - backend-serverless

backend-characterUpdater-build:
  image: golang
  stage: backend-characterUpdater-build
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends zip
  script:
    - cd backend/characterUpdater
    - make package

backend-serverless:
  image: node:11
  stage: backend-serverless
  dependencies:
    - backend-characterUpdater-build
  before_script:
    - cd backend
    - npm install
  script:
    - npm_modules/.bin/serverless deploy